# BOSS888 — HANDOFF (resume at home) · 2026-07-20

**One line:** Workstream 3 (agent/runtime sanitation) is COMPLETE and verified on staging. Everything deployable is live and reversible. The ONLY open item that needs YOU is giving me the current runtime source + Railway access so I can sanitise the runtime *internally* (it's already neutralised at the Laravel boundary). Then continue into W4.

---

## ▶️ READ ORDER WHEN YOU SIT DOWN
1. This file.
2. `BOSS888-MASTERCONTEXT-2026-07-20.md` — the whole program, classification, architecture, gotchas.
3. `BOSS888-W3-CHECKPOINT-2026-07-20.md` — the 24-item W3 evidence report.
4. `BOSS888-STATE.md` — running state (W3-complete banner is at the very top).

_All four are on staging `/var/www/levelup-staging/` AND local `C:\Users\markr\LVL\`._

---

## ✅ WHAT'S DONE (W3) — nothing to redo
- 10 removed agents + 6 constrained agents scrubbed from the authoritative layer (DB grants, roster APIs, routing maps, meeting candidates, proactive rules, Sarah prompts, capability accessors, seeders).
- `TaskService::create` launch-scope guard (memory-compat + routing safety net) — live-verified.
- `RuntimeClient` boundary sanitizer — the runtime still lists 20 agents, Laravel exposes only the 10 retained; verified live.
- 64/64 automated assertions + 4 live Sarah negative chats pass; 0 removed tasks/assignments created; Alex keeps all 26 Technical-SEO tools; workers 3/3; logs clean.
- Fully reversible: soft-disabled DB grants + `/root/backups/w3-capability-rollback.sql` + per-file `.bak-w3-*`.

## ⚠️ WHAT I NEED FROM YOU (the blocker)
To sanitise the runtime **inside** Railway (not just at the boundary), give me EITHER:
- **(a)** the current **v2.37.2 runtime source** (not the stale 2.36.0 zip — that would regress it), AND
- **(b)** **Railway deploy access** (`railway` CLI + `RAILWAY_TOKEN`, or the git repo Railway builds from).

Then I will: filter `capability-map.js` / `agents.js` / `registry.js` / `lu-planner.js` / `lu-agent-search.js` / `tool-registry.js` with the launch-scope list, rebuild, deploy, and re-run the runtime probes.
**If you'd rather not touch the runtime now:** it's safe — the boundary sanitizer + kernel deny + capability guard mean nothing removed can reach a user or execute. We can defer the in-runtime cleanup and keep moving.

## ⏭ NEXT: WORKSTREAM 4 — routes & public endpoints (unblocked, Laravel-side)
Goal: no public/route surface exposes or invokes removed social/email capability. Starting task list:
1. Grep `routes/*.php` for social/marketing/email/sequence/mention route groups + OAuth connect callbacks (Facebook/Instagram/LinkedIn/TikTok/X) + email open/click **tracking pixels** + webhook receivers.
2. For each: decide remove / return 404-or-"not in product" / keep-if-transactional. Do NOT break transactional email (password reset, receipts) — that's an explicit protect-list.
3. Check `routes/exec-api.php` + any public API that maps to a removed engine.
4. Backup → patch → `php -l` → cache:clear → verify with curl (authed + unauthed) → log-check.
Then W5 (article-share caption rebuild) → W6 (frontend) → W7 (marketing/plan truth) → W8 (dormant data) → regression → browser QA → smoke.
**Per the accepted plan: do NOT ask for another routine checkpoint — continue W4→W8 unless a genuine blocker/product decision emerges.**

## 🔁 RE-VERIFY CURRENT STATE IN ~2 MIN (paste-run to confirm nothing regressed)
```bash
# from local: scp then run — the two W3 verifier scripts still on staging /tmp are one-shot,
# but the fastest reconfirm is the live roster + a Sarah negative:
ssh staging 'cd /var/www/levelup-staging && SEC=$(grep -E "^RUNTIME_SECRET=" .env | cut -d= -f2- | tr -d "\"\r"); BASE=$(grep -E "^APP_URL=" .env | cut -d= -f2- | tr -d "\"\r"); curl -s -H "X-LevelUp-Secret: $SEC" "$BASE/api/internal/agents" | grep -oE "\"slug\":\"[a-z]+\"" | sort -u'
# expect exactly: alex diana elena james max nora priya ryan sarah sofia  (10, no removed)
```
Full re-verify scripts are in the local scratchpad and reproducible from the checkpoint report (§9).

## 🧰 ENVIRONMENT (don't re-derive)
- `ssh staging` → root@134.209.93.41; Laravel `/var/www/levelup-staging`; DB `levelup_staging`; app `https://staging.levelupgrowth.io` (curl `--resolve ...:443:127.0.0.1`).
- Runtime Railway v2.37.2; secret in `.env` (`X-LevelUp-Secret`). Test acct chef-red user2/ws2; JWT `php boss888-ni/mint.php 2 2`.
- tinker absent → bootstrap PHP manually. After edits: cache/config clear + fpm reload + `queue:restart` + `supervisorctl restart levelup-worker:`.
- **Gotchas:** ssh+nested-quotes → use `scp` a `.sh` then `bash /tmp/x.sh`; push files with `scp`; `tasks.source` is a constrained enum (keep `agent`, use payload markers for article-share); seeder must use `Agent::withRemovedAgents()` because of the model global scope.
- Working copy of the 22 edited files: `C:\Users\markr\LVL\w3-work\`.

## 🔙 IF YOU NEED TO ROLL BACK W3
- DB grants: `mysql levelup_staging < /root/backups/w3-capability-rollback.sql` (re-activates the revoked rows).
- Source: each file has `*.bak-w3-*` (and `*.bak-w3rt-*` / `*.bak-w3mem-*` / `*.bak-w3fix-*`) beside it; full pre-launch tar `/root/backups/prelaunch-src-20260719-221710.tar.gz`.
